# Uncomment this line to define a global platform for your project

platform :ios, '10.0'

source 'https://github.com/egouletlang/ios-BaseUtilityClassesSpecs.git'

target 'BaseUIClasses' do
  use_frameworks!

  # Pods for BaseUIClasses
  pod 'BaseUtilityClasses', :path => '~/Code/Pods/BaseUtilityClasses/'

  #pod 'BaseUtilityClasses', '~> 1.0.14'

  target 'BaseUIClassesTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
